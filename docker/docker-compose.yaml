version: "3"

services:
  workspace:
    container_name: workspace
    build:
      context: env
      dockerfile: Dockerfile
    ports:
      - 9000:9000
    tty: true
    volumes:
      - $HOME/workspace/scala_dev/muscle-training_manager:/root/muscle-training_manager
  db:
    image: postgres:latest
    container_name: postgres
    ports:
      - 5432:5432
    volumes:
      - ./postgres/init:/docker-entrypoint-initdb.d
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8"
    hostname: postgres
    restart: always
    user: postgres
